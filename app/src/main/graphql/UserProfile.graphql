query UserProfile($login:String!){
    repositoryOwner(login: $login) {
        ... on User {
            id
            avatarUrl
            name
            login
            bio
        }
    }
}
